#include "Util.h"
#include <algorithm>


    /*string stringHandle::ToLower(const string& sentence)
    {
        string uppercaseString;
        uppercaseString.resize(sentence.size());

        for (int i = 0; i < sentence.length(); i++)
        {
            static_cast<char>(uppercaseString[i]);
            uppercaseString[i] = tolower(static_cast<char>(sentence[i]));
        }

        return uppercaseString;
    }*/
